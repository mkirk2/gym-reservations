<%= "Gym member's list:" %>

<br>
<br>

<% @users.each do |user| %>
  <%= link_to(user.name, "/users/#{user.id}") %>
  
  <br>
  <!--<%= button_to "delete", user, :method => :delete %>-->
  
<% end %>

